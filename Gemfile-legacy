source "https://gem.coop"

gemspec

# This Gemfile is used in the CI environment for ruby < 3.0, where sorbet can't be loaded

# We also mnually require some old gem versions that work with ruby >= 2.0
gem "parallel", "1.13.0"
gem "ttfunk", "1.4.0"
gem "rdoc", "< 6.0.0"
gem "Ascii85", "< 2.0"
